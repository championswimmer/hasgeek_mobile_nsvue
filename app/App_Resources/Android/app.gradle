// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.hasgeek"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
